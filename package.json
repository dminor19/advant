{
    "name": "advant",
    "version": "1.0.0",
    "scripts": {
        "start": "npm --prefix ./server run start",
        "build": "cd web/ && npm install && npm run build-heroku && cd ../server/ && npm install && npm run build"
    },
    "license": "ISC"
}
