{
    "name": "advant",
    "version": "1.0.0",
    "scripts": {
        "start": "npm --prefix ./server run start",
        "build": "cd web/ && npm i && npm run build-heroku && cd ../server/ && npm i && npm run build"
    },
    "license": "ISC"
}
